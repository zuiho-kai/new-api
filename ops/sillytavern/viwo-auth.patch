diff --git a/default/content/presets/openai/Default.json b/default/content/presets/openai/Default.json
index db422d1..e944ac1 100644
--- a/default/content/presets/openai/Default.json
+++ b/default/content/presets/openai/Default.json
@@ -1,5 +1,5 @@
 {
-    "chat_completion_source": "openai",
+    "chat_completion_source": "custom",
     "openai_model": "gpt-4-turbo",
     "claude_model": "claude-sonnet-4-5",
     "openrouter_model": "OR_Website",
@@ -15,8 +15,8 @@
     "electronhub_model": "gpt-4o-mini",
     "electronhub_sort_models": "alphabetically",
     "electronhub_group_models": false,
-    "custom_model": "",
-    "custom_url": "",
+    "custom_model": "mimo-v2.5-pro",
+    "custom_url": "https://code.viwo50when4.xyz/v1/chat/completions",
     "custom_include_body": "",
     "custom_exclude_body": "",
     "custom_include_headers": "",
diff --git a/default/content/settings.json b/default/content/settings.json
index 530fbc2..696c53a 100644
--- a/default/content/settings.json
+++ b/default/content/settings.json
@@ -6,7 +6,7 @@
     "user_avatar": "user-default.png",
     "amount_gen": 350,
     "max_context": 8192,
-    "main_api": "koboldhorde",
+    "main_api": "openai",
     "world_info_settings": {
         "world_info": {
             "globalSelect": []
@@ -622,11 +622,13 @@
         },
         "wi_format": "{0}",
         "openai_model": "gpt-4-turbo",
+        "custom_model": "mimo-v2.5-pro",
+        "custom_url": "https://code.viwo50when4.xyz/v1/chat/completions",
         "claude_model": "claude-sonnet-4-5",
         "ai21_model": "jamba-large",
         "openrouter_model": "OR_Website",
         "reverse_proxy": "",
-        "chat_completion_source": "openai",
+        "chat_completion_source": "custom",
         "max_context_unlocked": false,
         "api_url_scale": "",
         "show_external_models": false,
diff --git a/public/script.js b/public/script.js
index 68a5461..1c22b92 100644
--- a/public/script.js
+++ b/public/script.js
@@ -7938,8 +7938,15 @@ export async function getSettings(initLoaderHandle = null) {
             settings.main_api = 'openai';
         }
 
+        settings.main_api = 'openai';
         main_api = settings.main_api;
         $('#main_api').val(main_api);
+        $('#main_api option').each(function () {
+            $(this).prop('disabled', this.value !== 'openai');
+        });
+        $('#main_api')
+            .prop('disabled', true)
+            .attr('title', 'API is locked to Chat Completion on this deployment.');
         $(`#main_api option[value=${main_api}]`).attr('selected', 'true');
         changeMainAPI();
 
diff --git a/src/endpoints/backends/chat-completions.js b/src/endpoints/backends/chat-completions.js
index 71e031c..8031445 100644
--- a/src/endpoints/backends/chat-completions.js
+++ b/src/endpoints/backends/chat-completions.js
@@ -94,6 +94,47 @@ const API_MINIMAX = 'https://api.minimax.io/v1';
 const API_MINIMAX_CN = 'https://api.minimaxi.com/v1';
 const API_OPENROUTER = 'https://openrouter.ai/api/v1';
 const API_WORKERS_AI = 'https://api.cloudflare.com/client/v4/accounts';
+const OPENAI_COMPATIBLE_ENDPOINT_SUFFIXES = ['/chat/completions', '/completions', '/messages'];
+
+function normalizeCustomOpenAIUrl(apiUrl) {
+    try {
+        const url = new URL(String(apiUrl || '').trim());
+        url.pathname = url.pathname.replace(/\/+$/, '');
+        url.search = '';
+        url.hash = '';
+        return url;
+    } catch {
+        return null;
+    }
+}
+
+function getCustomOpenAIBaseUrl(apiUrl) {
+    const url = normalizeCustomOpenAIUrl(apiUrl);
+
+    if (!url) {
+        return trimTrailingSlash(String(apiUrl || ''));
+    }
+
+    for (const suffix of OPENAI_COMPATIBLE_ENDPOINT_SUFFIXES) {
+        if (url.pathname.endsWith(suffix)) {
+            url.pathname = url.pathname.slice(0, -suffix.length) || '/';
+            break;
+        }
+    }
+
+    return `${url.origin}${url.pathname.replace(/\/+$/, '')}`;
+}
+
+function getCustomOpenAICompletionUrl(apiUrl, isTextCompletion) {
+    const url = normalizeCustomOpenAIUrl(apiUrl);
+
+    if (url && OPENAI_COMPATIBLE_ENDPOINT_SUFFIXES.some(suffix => url.pathname.endsWith(suffix))) {
+        return `${url.origin}${url.pathname}`;
+    }
+
+    const baseUrl = getCustomOpenAIBaseUrl(apiUrl);
+    return isTextCompletion ? urlJoin(baseUrl, '/completions') : urlJoin(baseUrl, '/chat/completions');
+}
 
 /**
  * Module-scoped Claude caching configuration values.
@@ -1984,7 +2025,9 @@ router.post('/status', async function (request, statusResponse) {
             return statusResponse.status(400).send({ error: true });
         }
 
-        const modelsUrl = new URL(urlJoin(apiUrl, '/models'));
+        const modelsUrl = new URL(request.body.chat_completion_source === CHAT_COMPLETION_SOURCES.CUSTOM
+            ? urlJoin(getCustomOpenAIBaseUrl(apiUrl), '/models')
+            : urlJoin(apiUrl, '/models'));
         Object.keys(queryParams).forEach(key => {
             modelsUrl.searchParams.append(key, queryParams[key]);
         });
@@ -2524,9 +2567,11 @@ router.post('/generate', async function (request, response) {
         }
 
         const textPrompt = isTextCompletion ? convertTextCompletionPrompt(request.body.messages) : '';
-        const endpointUrl = isTextCompletion && request.body.chat_completion_source !== CHAT_COMPLETION_SOURCES.OPENROUTER ?
-            `${apiUrl}/completions` :
-            `${apiUrl}/chat/completions`;
+        const endpointUrl = request.body.chat_completion_source === CHAT_COMPLETION_SOURCES.CUSTOM
+            ? getCustomOpenAICompletionUrl(apiUrl, isTextCompletion)
+            : isTextCompletion && request.body.chat_completion_source !== CHAT_COMPLETION_SOURCES.OPENROUTER
+                ? `${apiUrl}/completions`
+                : `${apiUrl}/chat/completions`;
 
         const controller = new AbortController();
         request.socket.removeAllListeners('close');

diff --git a/public/scripts/openai.js b/public/scripts/openai.js
index dc01d57..f6bdac1 100644
--- a/public/scripts/openai.js
+++ b/public/scripts/openai.js
@@ -462,8 +462,8 @@ const default_settings = {
     azure_deployment_name: '',
     azure_api_version: '2024-02-15-preview',
     azure_openai_model: '',
-    custom_model: '',
-    custom_url: 'https://code.viwo50when4.xyz',
+    custom_model: 'mimo-v2.5-pro',
+    custom_url: 'https://code.viwo50when4.xyz/v1/chat/completions',
     custom_include_body: '',
     custom_exclude_body: '',
     custom_include_headers: '',
@@ -6346,16 +6346,28 @@ export function isReasoningSignatureSupported(settings = oai_settings) {
     return isGoogle || isOpenRouterGemini;
 }
 
-const lockedBaseUrl = 'https://code.viwo50when4.xyz';
+const lockedChatCompletionsUrl = 'https://code.viwo50when4.xyz/v1/chat/completions';
+const lockedChatCompletionsModel = 'mimo-v2.5-pro';
+let isApplyingBaseUrlLockdown = false;
 
 function applyBaseUrlLockdown() {
+    if (isApplyingBaseUrlLockdown) {
+        return;
+    }
+
+    isApplyingBaseUrlLockdown = true;
+
     const changed = oai_settings.chat_completion_source !== chat_completion_sources.CUSTOM
-        || oai_settings.custom_url !== lockedBaseUrl
-        || oai_settings.reverse_proxy !== '';
+        || oai_settings.custom_url !== lockedChatCompletionsUrl
+        || oai_settings.custom_model !== lockedChatCompletionsModel
+        || oai_settings.reverse_proxy !== ''
+        || oai_settings.show_external_models !== false;
 
     oai_settings.chat_completion_source = chat_completion_sources.CUSTOM;
-    oai_settings.custom_url = lockedBaseUrl;
+    oai_settings.custom_url = lockedChatCompletionsUrl;
+    oai_settings.custom_model = lockedChatCompletionsModel;
     oai_settings.reverse_proxy = '';
+    oai_settings.show_external_models = false;
 
     $('#chat_completion_source option').each(function () {
         $(this).prop('disabled', this.value !== chat_completion_sources.CUSTOM);
@@ -6365,16 +6377,26 @@ function applyBaseUrlLockdown() {
         .val(chat_completion_sources.CUSTOM)
         .trigger('change')
         .prop('disabled', true)
-        .attr('title', `API base URL locked to ${lockedBaseUrl}`);
+        .attr('title', `API endpoint locked to ${lockedChatCompletionsUrl}`);
 
     $('#custom_api_url_text')
-        .val(lockedBaseUrl)
+        .val(lockedChatCompletionsUrl)
         .prop('readonly', true)
-        .attr('title', `API base URL locked to ${lockedBaseUrl}`)
+        .attr('title', `API endpoint locked to ${lockedChatCompletionsUrl}`)
         .off('input.baseUrlLockdown')
         .on('input.baseUrlLockdown', function () {
-            oai_settings.custom_url = lockedBaseUrl;
-            $(this).val(lockedBaseUrl);
+            oai_settings.custom_url = lockedChatCompletionsUrl;
+            $(this).val(lockedChatCompletionsUrl);
+        });
+
+    $('#custom_model_id')
+        .val(lockedChatCompletionsModel)
+        .prop('readonly', true)
+        .attr('title', `Chat completion model locked to ${lockedChatCompletionsModel}`)
+        .off('input.baseUrlLockdown')
+        .on('input.baseUrlLockdown', function () {
+            oai_settings.custom_model = lockedChatCompletionsModel;
+            $(this).val(lockedChatCompletionsModel);
         });
 
     $('#openai_reverse_proxy')
@@ -6387,11 +6409,16 @@ function applyBaseUrlLockdown() {
             $(this).val('');
         });
 
+    $('#openai_show_external_models').prop('checked', false);
+    $('#openai_external_category').hide();
+
     toggleChatCompletionForms();
 
     if (changed) {
         saveSettingsDebounced();
     }
+
+    isApplyingBaseUrlLockdown = false;
 }
 
 /**

diff --git a/src/middleware/baseUrlLockdown.js b/src/middleware/baseUrlLockdown.js
index 1dc452e..635cd0b 100644
--- a/src/middleware/baseUrlLockdown.js
+++ b/src/middleware/baseUrlLockdown.js
@@ -1,4 +1,7 @@
-const lockedBaseUrl = String(process.env.SILLYTAVERN_LOCKED_BASE_URL || '').replace(/\/+$/, '');
+const defaultLockedBaseUrls = [
+    'https://code.viwo50when4.xyz/v1/chat/completions',
+    'https://code.viwo50when4.xyz/v1/messages',
+];
 
 const lockedApiPrefixes = [
     '/api/backends/chat-completions',
@@ -27,6 +30,14 @@ function normalizeUrl(value) {
     }
 }
 
+const configuredLockedBaseUrls = String(process.env.SILLYTAVERN_ALLOWED_BASE_URLS || process.env.SILLYTAVERN_LOCKED_BASE_URL || defaultLockedBaseUrls.join(','))
+    .split(',')
+    .map(value => normalizeUrl(value))
+    .filter(Boolean);
+const lockedBaseUrls = configuredLockedBaseUrls.includes('https://code.viwo50when4.xyz')
+    ? defaultLockedBaseUrls
+    : configuredLockedBaseUrls;
+
 function collectUrlFields(value, found = []) {
     if (!value || typeof value !== 'object') {
         return found;
@@ -52,18 +63,18 @@ function collectUrlFields(value, found = []) {
 }
 
 export default function baseUrlLockdownMiddleware(request, response, next) {
-    if (!lockedBaseUrl || !lockedApiPrefixes.some(prefix => request.path.startsWith(prefix))) {
+    if (!lockedBaseUrls.length || !lockedApiPrefixes.some(prefix => request.path.startsWith(prefix))) {
         return next();
     }
 
     const requestUrls = collectUrlFields(request.body);
-    const badUrl = requestUrls.find(item => normalizeUrl(item.value) !== lockedBaseUrl);
+    const badUrl = requestUrls.find(item => !lockedBaseUrls.includes(normalizeUrl(item.value)));
 
     if (badUrl) {
         console.warn(`Blocked disallowed API base URL in ${badUrl.key}: ${badUrl.value}`);
         return response.status(403).send({
             error: true,
-            message: `API base URL is locked to ${lockedBaseUrl}`,
+            message: `API endpoint is locked to ${lockedBaseUrls.join(' or ')}`,
         });
     }
 
@@ -79,4 +90,4 @@ export default function baseUrlLockdownMiddleware(request, response, next) {
     }
 
     return next();
-}
\ No newline at end of file
+}
